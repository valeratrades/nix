# https://manpages.ubuntu.com/manpages/xenial/man5/zathurarc.5.html
# https://manpages.ubuntu.com/manpages/xenial/man1/zathura.1.html

# s to fill width of the window
# reload on z
# quit on q
# zoom in or out on + and -
# <C-r> to recolor

# F5 for presentation-mode


map s scroll left
map r scroll down
map n scroll up
map t scroll right

map d scroll half-down
map u scroll half-up
map <C-d> toggle_page_mode
# I think `u` wasn't taken

map j rotate
map z reload

map <C-\=> zoom in
map \= zoom in
map <C-\-> zoom out
map <A-q> quit
map <A-;> quit

map <A-o> jumplist backward
map <A-i> jumplist forward

map j search next
map k search prev

#TODO: make into a macro, following this with few `+`es. Then have another shortcut for `f` with `-`es, for when it's being opened in vertical split (zathura is not smart enough)
map f zoom width

# General
map y copy
set selection-clipboard clipboard
set adjust-open "best-fit"
set recolor true

#set font "{{ font.monospace }} {{ font.size }}"
