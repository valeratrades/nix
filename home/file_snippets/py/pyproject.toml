[project]
name = "PROJECT_NAME_PLACEHOLDER"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [
    { name = "TODO", email = "TODO@example.com" },
]
urls = { Documentation = "https://github.com/GITHUB_USER/PROJECT_NAME_PLACEHOLDER#readme", Issues = "https://github.com/GITHUB_USER/PROJECT_NAME_PLACEHOLDER/issues", Source = "https://github.com/GITHUB_USER/PROJECT_NAME_PLACEHOLDER" }

dependencies = [
    "loguru>=0.7.0",
    "typeguard>=4.4.4",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[dependency-groups]
dev = [
    "pytest>=6.0",
    "ruff>=0.1.0",
    "icecream>=2.1.8",
    "inline-snapshot>=0.17",
]
